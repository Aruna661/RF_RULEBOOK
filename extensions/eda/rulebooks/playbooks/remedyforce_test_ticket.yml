---
- name: Handle Remedyforce test ticket
  hosts: localhost
  gather_facts: false
  vars:
    incident_id: "{{ incident_id }}"
    incident_number: "{{ incident_number | default('') }}"
  tasks:
    - name: Log incident info
      ansible.builtin.debug:
        msg: "Triggering automation for Incident {{ incident_number }} (Id={{ incident_id }})"

    # TODO: add your remediation tasks here
    # e.g., call modules, workflows, or other automations
