---
- name: Handle Remedyforce test ticket
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Log incident info
      ansible.builtin.debug:
        msg: "Triggering automation for task"

    # TODO: add your remediation tasks here
    # e.g., call modules, workflows, or other automations
